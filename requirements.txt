# Core API
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.17
pydantic==2.10.3
pydantic-settings==2.6.1
typing_extensions>=4.12.2

# PyTorch 2.2 + sentence-transformers: NumPy 2.x breaks embedding (.numpy() / ABI); keep 1.x.
numpy>=1.26.4,<2.0.0

# Documents
pymupdf==1.24.14
pypdf==5.1.0
pillow==10.4.0

# OCR (optional at runtime; install Tesseract on host for full OCR)
pytesseract==0.3.13

# Retrieval
chromadb==0.5.23
sentence-transformers==3.3.1
rank-bm25==0.2.2

# Storage
sqlmodel==0.0.22
aiosqlite==0.20.0

# LLM-backed drafting (Gemini REST client)
httpx==0.28.1

# Dev / tests
pytest==8.3.4
pytest-asyncio==0.24.0

# Sample PDF script only (lightweight; avoids requiring PyMuPDF for scripts/build_sample_pdf.py)
fpdf2==2.8.2
